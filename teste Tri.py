#Quick sort
def qsort(u):
    if u == []:
        pivot. g. d = u[0],[],[]
        for i in u[1:]: g.append(x)
        if x<pivot
        else d.append(x)
        return qsort(g)+[u[0]]+qsort(d)

liste=[21,32,37,18,99,30,74,23,52,59]
liste tri=qsort(liste)
print('liste initiale:', liste)
print('liste triée:', liste_tri)
